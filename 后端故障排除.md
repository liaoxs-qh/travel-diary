# 后端启动故障排除

## 🔴 问题：Application run failed
**错误信息**: `java.lang.IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType': java.lang.String`

**原因**: MyBatis-Plus 版本与 Spring Boot 3 不兼容。

**✅ 解决方案**: 
已在 `pom.xml` 中将依赖更新为 `mybatis-plus-spring-boot3-starter`。

请重新运行：
```bash
cd server
mvn clean spring-boot:run
```

---

## 🟡 其他常见问题

### 1. 数据库连接失败
**错误**: `Communications link failure`
**解决**: 
1. 确保 MySQL 服务已启动
2. 检查 `application-dev.yml` 中的密码是否正确
3. 确保数据库 `travel_diary` 已创建

### 2. 端口被占用
**错误**: `Web server failed to start. Port 8080 was already in use.`
**解决**:
修改 `application-dev.yml`：
```yaml
server:
  port: 8081 # 改为其他端口
```

### 3. Redis 连接失败
**错误**: `Unable to connect to Redis`
**解决**:
如果本地没有 Redis，可以在 `application-dev.yml` 中暂时禁用 Redis 相关配置（或者安装并启动 Redis）。
*本项目代码中依赖 Redis 缓存，建议安装 Redis*。

如果实在不想装 Redis，可以注释掉 `com.travel.diary.config.RedisConfig` 相关代码，并修改业务逻辑。

---

## 🟢 验证启动成功

启动成功后，你会看到类似日志：
```
Tomcat started on port 8080 (http) with context path ''
Started TravelDiaryApplication in 2.456 seconds (process running for 2.89)
```

然后访问：http://localhost:8080/doc.html
